apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerting-monitoring-logging.fullname" . }}-test-connection"
  labels:
    {{- include "alerting-monitoring-logging.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerting-monitoring-logging.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
